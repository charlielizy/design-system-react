import { CSSProp } from "styled-components";

export interface IIcons {
  title?: string;
  icon: string;
  size?: string | number;
  style?: any;
  fill?: string;
  block?: boolean;
  className?: string;
}
